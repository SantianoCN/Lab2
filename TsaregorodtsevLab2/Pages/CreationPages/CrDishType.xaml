<Page x:Class="TsaregorodtsevLab2.Pages.CrDishType"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Создание типа блюда">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Создание типа блюда" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1">
            <Label Content="Название типа:"/>
            <TextBox x:Name="NameTextBox" Margin="0,5,0,15" Height="25"/>
        </StackPanel>

        <Button Grid.Row="2" x:Name="CreateButton" Content="Создать" Width="120" Height="30" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="CreateButton_Click"/>
    </Grid>
</Page>
